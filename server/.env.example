# Podcast Index credentials (https://api.podcastindex.org) — leave empty to
# disable directory search; everything else still works.
PODCASTINDEX_KEY=
PODCASTINDEX_SECRET=

DATABASE_PATH=data/pods.sqlite
BIND_ADDR=0.0.0.0:8080
RUST_LOG=pods_server=info
